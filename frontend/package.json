{
  "name": "frontend",
  "version": "1.0.0",
  "description": "",
  "main": "index.html",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "tsc --noEmit && parcel index.html --global customModule --port 3000",
    "build": "rm -rf dist/* && tsc --noEmit && parcel build index.html --global customModule"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.21.1"
  },
  "devDependencies": {
    "@parcel/config-default": "^2.0.0-alpha.3",
    "@parcel/transformer-typescript-tsc": "^2.0.0-alpha.3",
    "@types/axios": "^0.14.0",
    "@types/parcel-env": "0.0.0",
    "parcel-bundler": "^1.12.5",
    "typescript": "^4.2.4"
  }
}
