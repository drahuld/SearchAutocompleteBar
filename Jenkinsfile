pipeline {
    agent any
    environment {
            PATH = "$PATH:/usr/bin"
    }
    stages {
        stage('Hello World') {
            steps {
               echo 'Hello World from Repository'
            }
        }
    }
}
